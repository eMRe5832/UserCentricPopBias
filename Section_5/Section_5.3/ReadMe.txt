You should first apply Aug, Mul and xQuad algorithms via VaR_ERPs.mat and xQuad.mat files and stores results as the DB-DebiasingName.mat. Then, you should such constructed .mat files when evaluating the results via Main.mat file. 